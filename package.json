{
    "name": "lambda authorize function",
    "description": "AWS Lambda function for wrapper of AWS",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "https": "^1.0.0"
    }
}
